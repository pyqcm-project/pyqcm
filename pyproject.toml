[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "scikit-build>=0.12",
    "numpy<2",
    "ninja"
]

build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
