image: python:3.10

pipelines:
  tags:
    '*':
      - step:
          name: Deploy
          deployment: Production
          script:
            - pip install setuptools==78.1.0
            - pip install scikit-build==0.17.6 twine==6.1.0
            - python setup.py sdist
            - python -m twine upload dist/*
